// Para uso en componentes del cliente
export { supabaseClient } from "./client";

// Para uso en componentes del servidor
// Importa directamente desde './server' cuando necesites el servidor client

// Re-export tipos comunes
export type { Database } from "./types";
